<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.mapper.AttendanceMapper">

    <resultMap id="attendanceMap" type="com.attendance.entity.Attendance">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="type" column="type"/>
        <result property="latitude" column="latitude"/>
        <result property="longitude" column="longitude"/>
        <result property="address" column="address"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.attendance.entity.Attendance" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO attendance (user_id, type, latitude, longitude, address, status, remark)
        VALUES (#{userId}, #{type}, #{latitude}, #{longitude}, #{address}, #{status}, #{remark})
    </insert>

    <select id="selectByUserId" resultMap="attendanceMap">
        SELECT * FROM attendance WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="selectByUserIdAndDate" resultMap="attendanceMap">
        SELECT * FROM attendance
        WHERE user_id = #{userId} AND DATE(create_time) = #{date}
        ORDER BY create_time DESC
    </select>

    <select id="selectAll" resultMap="attendanceMap">
        SELECT * FROM attendance ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM attendance
    </select>

    <select id="selectById" resultMap="attendanceMap">
        SELECT * FROM attendance WHERE id = #{id}
    </select>

    <update id="updateById" parameterType="com.attendance.entity.Attendance">
        UPDATE attendance SET
            status = #{status},
            remark = #{remark}
        WHERE id = #{id}
    </update>

</mapper>
