<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.mapper.UserMapper">

    <resultMap id="userMap" type="com.attendance.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="departmentId" column="department_id"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByUsername" resultMap="userMap">
        SELECT * FROM t_user WHERE username = #{username}
    </select>

    <select id="selectById" resultMap="userMap">
        SELECT * FROM t_user WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.attendance.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_user (username, password, real_name, phone, email, role, status, department_id, create_time, update_time)
        VALUES (#{username}, #{password}, #{realName}, #{phone}, #{email}, #{role}, #{status}, #{departmentId}, NOW(), NOW())
    </insert>

    <update id="updateById" parameterType="com.attendance.entity.User">
        UPDATE t_user SET
            real_name = #{realName},
            phone = #{phone},
            email = #{email},
            update_time = NOW()
        WHERE id = #{id}
    </update>

</mapper>
